{
  "buildCommand": "pnpm install --no-frozen-lockfile && pnpm build",
  "outputDirectory": "dist/public",
  "framework": "vite",
  "routes": [
    { "src": "^/api/(.*)", "dest": "/api/$1" },
    { "src": "^(.*)$", "dest": "/index.html" }
  ]
}
